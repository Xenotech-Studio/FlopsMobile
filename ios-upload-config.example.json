{
  "$schema_note": "TestFlight / App Store 上传配置模板。复制为 ios-upload-config.json 并填入真实值。该 example 文件进 git；ios-upload-config.json 在 .gitignore 中。",
  "$source_note": "ASC_API_KEY_ID 和 ASC_ISSUER_ID 从 App Store Connect → Users and Access → Integrations 获取。Key 角色推荐 App Manager（最小可用权限）。.p8 文件下载后放到 FlopsMobile/secrets/ 目录。",

  "ASC_API_KEY_ID": "REPLACE_ME_10_CHAR_KEY_ID",
  "ASC_ISSUER_ID": "REPLACE_ME_UUID_ISSUER_ID",
  "ASC_API_KEY_PATH": "secrets/AuthKey_REPLACE_ME.p8"
}
